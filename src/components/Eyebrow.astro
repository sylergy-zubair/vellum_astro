---
/* Green uppercase label. */
interface Props {
  children: string;
  tone?: "default" | "ink";
}
const { children, tone = "default" } = Astro.props;
---
<span class={`eyebrow eyebrow--${tone}`}>{children}</span>

<style>
  .eyebrow--ink {
    color: var(--green-bright);
  }
</style>
